# =============================================================================
# MEDUSA BACKEND - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env files to git!

# -----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# -----------------------------------------------------------------------------
# IMPORTANT: Use the SAME database URL as rythumowas.shop/.env
# Both frontend and backend share the same database
#
# Get this from:
# - Neon: https://neon.tech (recommended)
# - Supabase: https://supabase.com
# - Local: postgresql://user:password@localhost:5432/rythumowa

DATABASE_URL="postgresql://user:password@localhost:5432/rythumowa"

# -----------------------------------------------------------------------------
# REDIS (Required for Medusa)
# -----------------------------------------------------------------------------
# Redis is used for caching, sessions, and event bus
#
# Option 1: Local Redis (Development)
#   Windows: Download from https://github.com/microsoftarchive/redis/releases
#   Mac: brew install redis
#   Linux: sudo apt install redis
#   Then: redis-server
#   URL: redis://localhost:6379
#
# Option 2: Upstash (Free tier, no setup)
#   1. Go to https://upstash.com
#   2. Create free account
#   3. Create Redis database
#   4. Copy connection string
#   Example: redis://default:password@xxx.upstash.io:6379
#
# Option 3: Redis Cloud (Free tier)
#   1. Go to https://redis.com/try-free
#   2. Create database
#   3. Copy connection string

REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# -----------------------------------------------------------------------------
# Allow requests from these origins
# Development: Use localhost URLs
# Production: Use your actual domains

# Frontend URL (rythumowas.shop)
STORE_CORS="http://localhost:3000"

# Medusa Admin Panel URL
ADMIN_CORS="http://localhost:9000"

# -----------------------------------------------------------------------------
# JWT & COOKIES (Security)
# -----------------------------------------------------------------------------
# Generate secure random strings for these
# Run: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32

JWT_SECRET="your-jwt-secret-here-generate-with-openssl"
COOKIE_SECRET="your-cookie-secret-here-generate-with-openssl"

# -----------------------------------------------------------------------------
# OPTIONAL: Admin Panel
# -----------------------------------------------------------------------------
# Auto-open admin panel in browser when starting dev server
# Set to false if you don't want this

OPEN_BROWSER=true

# -----------------------------------------------------------------------------
# NODE ENVIRONMENT
# -----------------------------------------------------------------------------
# development | production | test

NODE_ENV=development
